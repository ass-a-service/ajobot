version: "3"

services:
  bot:
    build: .
    restart: always
    env_file: .env
    depends_on:
      - redis
  redis:
    image: redis:6.2
    restart: unless-stopped
    command: redis-server --appendonly yes --loglevel warning
    ports:
      - '6379:6379'
